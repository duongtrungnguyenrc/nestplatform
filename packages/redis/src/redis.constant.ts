/** Injection token for the Redis configuration object. */
export const REDIS_CONFIG = "token:redis-config";

/** Default injection token for the Redis client instance. */
export const REDIS_CLIENT = "token:redis-client";
